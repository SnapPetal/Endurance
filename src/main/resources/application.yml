spring:
  # Application Identity
  application:
    name: endurance

  # Database Configuration
  datasource:
    url: jdbc:postgresql://ls-3cd9e093f05f5c4d801b58031197f04063b55800.cmldxjsfqvn4.us-east-1.rds.amazonaws.com:5432/dbmaster
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.xml
    url: ${spring.datasource.url}
    user: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    liquibaseSchemaName: endurance
    commmand:
      defaultSchemaName: endurance
  ai:
    bedrock:
      aws:
        region: us-east-1
        access-key: ${AWS_ACCESS_KEY_ID}
        secret-key: ${AWS_SECRET_ACCESS_KEY}
    chat:
      client:
        enabled: false

# Server Configuration
server:
  port: 8080
  servlet:
    context-path: /
  forward-headers-strategy: native

# Logging Configuration
logging:
  level:
    org.springframework.jdbc: DEBUG
    org.liquibase: DEBUG
    com.zaxxer.hikari: DEBUG
    org.postgresql: DEBUG
    com.thonbecker.endurance: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"

# Monitoring/Actuator Configuration
management:
  endpoints:
    web:
      exposure:
        include: "*"
  health:
    default-timeout: 180s  # 3 minutes
    db:
      timeout: 60s  # 1 minute for database health
    diskspace:
      timeout: 30s
    redis:
      timeout: 30s
  endpoint:
    health:
      show-details: when-authorized
      show-components: always